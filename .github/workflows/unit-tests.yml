name: "Unit tests"

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        name:
      - uses: actions/checkout@v4
        name: update node
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm ci 
      - run: yarn test
